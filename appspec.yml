version: 0.0
os: linux

file_exists_behavior: OVERWRITE

files:
  - source: /
    destination: /var/www/html

hooks:
  ApplicationStop:
    - location: remove.sh
      timeout: 300
      runas: root
      
  AfterInstall:
    - location: deploy.sh
      timeout: 300
      runas: root